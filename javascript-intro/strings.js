const firstName = 'John'
const lastName = 'Smith'

//  string concatenation

const concat = "Hello " + firstName + " " + lastName + "! How are you?!"

// string interpolation - template strings

const interp = `Hello ${firstName} ${lastName}! How are you?!`

console.log(concat);
console.log(interp);